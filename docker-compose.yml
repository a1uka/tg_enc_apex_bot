version: '3.8'

services:
  algs-bot:
    build: .
    container_name: algs-telegram-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - BOT_TOKEN=${BOT_TOKEN}